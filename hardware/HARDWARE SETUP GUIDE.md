# Base Revolt 하드웨어 설치 가이드

## 📋 개요

Base Revolt v2.0은 **분리 아키텍처**를 사용합니다:
- **카메라 보드**: ESP32-CAM (OV2640) 또는 ESP32-S3 (OV3660)
- **조종 보드**: ESP32-C3 SuperMini (통일)

각 보드는 독립적으로 작동하며, WebSocket 서버를 통해 통신합니다.

---

## 🔧 v2.0 아키텍처 (현재 버전)

### 카메라 보드 선택

#### 옵션 1: ESP32-CAM + OV2640 (저가형)
- 폴더: `ESP32 cam + OV2640 Cam/`
- 펌웨어: `esp32cam_camera.ino`
- Tools 세팅: `ESP32_CAM_BOARD_SETTINGS.md`

#### 옵션 2: ESP32-S3 N16R8 + OV3660 (고가형)
- 폴더: `ESP32-S3 N16R8 + OV3660 Cam/`
- 펌웨어: `esp32s3_camera.ino`
- Tools 세팅: `ESP32_S3_BOARD_SETTINGS.md`

### 조종 보드 (통일)

- **ESP32-C3 SuperMini** (모든 카메라 모델과 호환)
- 펌웨어: `esp32c3_control.ino`
- Tools 세팅: `ESP32_C3_BOARD_SETTINGS.md`

---

## 📚 상세 가이드

각 보드별 상세 설정은 다음 문서를 참고하세요:

1. **ESP32-C3 SuperMini 조종 보드**
   - `ESP32_C3_BOARD_SETTINGS.md` - Tools 세팅 및 GPIO 핀맵
   - `esp32c3_control.ino` - 펌웨어 코드

2. **ESP32-CAM 카메라 보드 (저가형)**
   - `ESP32 cam + OV2640 Cam/ESP32_CAM_BOARD_SETTINGS.md` - Tools 세팅
   - `ESP32 cam + OV2640 Cam/esp32cam_camera.ino` - 펌웨어 코드

3. **ESP32-S3 카메라 보드 (고가형)**
   - `ESP32-S3 N16R8 + OV3660 Cam/ESP32_S3_BOARD_SETTINGS.md` - Tools 세팅
   - `ESP32-S3 N16R8 + OV3660 Cam/esp32s3_camera.ino` - 펌웨어 코드

4. **통합 가이드**
   - `../하드웨어_설치_가이드.md` - 전체 시스템 설치 가이드 (한국어/영어)

---

## 🔌 L298N 모터 드라이버 연결

### ESP32-C3 SuperMini → L298N

```
ESP32-C3 SuperMini → L298N
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GPIO 3              → IN1 (구동 모터)
GPIO 4              → IN2 (구동 모터)
GPIO 6              → IN3 (스티어링 모터)
GPIO 7              → IN4 (스티어링 모터)
5V                  → 5V (로직 전원)
GND                 → GND

L298N → 모터 & 배터리
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUT1, OUT2          → 구동 모터 (앞뒤 바퀴)
OUT3, OUT4          → 스티어링 모터 (앞바퀴 방향)
VIN (12V)           → 배터리 + (7-12V)
GND                 → 배터리 -
ENA 점퍼            → ON (점퍼 연결)
ENB 점퍼            → ON (점퍼 연결)
```

### 조종 방식

- **일반 RC카 방식**: 구동(앞뒤) + 스티어링(좌우) 독립 제어
- **W**: 전진, **S**: 후진, **A**: 좌회전, **D**: 우회전, **Space**: 정지

---

## ⚠️ 레거시 파일 (v1.0 구버전)

다음 파일들은 **v1.0 구버전** (통합형 아키텍처)이며, 현재는 사용하지 않습니다:

- `esp32_rc_car.ino` - ESP32-CAM 통합형 (카메라+조종)
- `esp32_rc_car_standalone.ino` - 독립형 테스트용
- `esp32s_control.ino` - ESP-32S용 조종 (구버전, 삭제 예정)

**참고**: v2.0에서는 카메라와 조종이 분리되어 있으며, 조종 보드는 ESP32-C3 SuperMini로 통일됩니다.

---

## 📦 필요한 부품

### 필수 부품
- **카메라 보드**: ESP32-CAM (OV2640) 또는 ESP32-S3 (OV3660)
- **조종 보드**: ESP32-C3 SuperMini
- **L298N 모터 드라이버** (듀얼 H-브리지)
- **DC 모터 2개** (구동 모터 + 스티어링 모터)
- **RC카 섀시** (바디)
- **배터리** (7.4V ~ 12V)
- **점퍼 와이어** (M-M, M-F)

### 추가 부품 (선택사항)
- **LED** (상태 표시용)
- **부저** (알림용)
- **스위치** (전원용)

---

## 🛠️ 문제 해결

### 모터가 안 움직일 때
1. 전원 연결 확인
2. 모터 극성 확인
3. 제어 신호 확인
4. L298N 점퍼 확인

### WiFi 연결 실패
1. SSID/비밀번호 확인
2. 2.4GHz WiFi만 지원 (5GHz 불가)
3. 신호 강도 확인

### WebSocket 연결 실패
1. 서버 주소 확인 (도메인만, https:// 제외)
2. 서버가 실행 중인지 확인
3. 네트워크 방화벽 확인

### 카메라 초기화 실패
1. 5V 전원 확인 (3.3V는 부족)
2. 카메라 모듈 연결 확인 (FPC 케이블)
3. 핀맵 확인 (보드별로 다름)

---

## 📞 지원

문제가 발생하면 다음을 확인하세요:
1. 각 보드별 Tools 세팅 문서 확인
2. 펌웨어 코드의 TODO 주석 확인
3. 시리얼 모니터 로그 확인
4. 하드웨어 연결 확인

---

**작성일**: 2025-11-13  
**버전**: v2.0  
**아키텍처**: 분리형 (카메라 + 조종 독립)

